/*
Test generated by RoostGPT for test pom-test using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

Sure, here are some test scenarios that you can use to validate the business logic of the `getName` method:

1. **Test with a null name**:
   - Set the `name` field to null.
   - Call the `getName` method and verify that it returns null.

2. **Test with an empty string**:
   - Set the `name` field to an empty string.
   - Call the `getName` method and verify that it returns an empty string.

3. **Test with a non-empty string**:
   - Set the `name` field to a non-empty string.
   - Call the `getName` method and verify that it returns the same string.

4. **Test with a name containing special characters**:
   - Set the `name` field to a string containing special characters.
   - Call the `getName` method and verify that it returns the same string.

5. **Test with a name containing numbers**:
   - Set the `name` field to a string containing numbers.
   - Call the `getName` method and verify that it returns the same string.

6. **Test with a name containing spaces**:
   - Set the `name` field to a string containing spaces.
   - Call the `getName` method and verify that it returns the same string.

Remember, these tests are based on the assumption that the `name` field is not null and is a string. If the `name` field can be null or a different type, you will need to adjust the tests accordingly.

*/
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.springframework.samples.petclinic.model.NamedEntity;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class NamedEntity_getName_8400ac6fb7_Test {

    @Test
    public void testGetName_success() {
        // Arrange
        String expectedName = "Test Name";
        NamedEntity namedEntity = new NamedEntity();
        namedEntity.setName(expectedName);

        // Act
        String actualName = namedEntity.getName();

        // Assert
        assertEquals(expectedName, actualName);
    }

    @Test
    public void testGetName_null() {
        // Arrange
        NamedEntity namedEntity = new NamedEntity();

        // Act
        String actualName = namedEntity.getName();

        // Assert
        assertEquals(null, actualName);
    }
}
