/*
Test generated by RoostGPT for test pom-test using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

Sure, here are some test scenarios that you can use to validate the business logic of the `setFirstName` method:

1. **Test with a valid first name**:
   - Input: "John"
   - Expected Output: The `firstName` field should be set to "John".

2. **Test with an empty string**:
   - Input: ""
   - Expected Output: The `firstName` field should not be set.

3. **Test with a null value**:
   - Input: null
   - Expected Output: The `firstName` field should not be set.

4. **Test with a string containing only spaces**:
   - Input: "   "
   - Expected Output: The `firstName` field should not be set.

5. **Test with a string containing only tabs**:
   - Input: "\t\t\t"
   - Expected Output: The `firstName` field should not be set.

6. **Test with a string containing only newlines**:
   - Input: "\n\n\n"
   - Expected Output: The `firstName` field should not be set.

7. **Test with a string containing only carriage returns**:
   - Input: "\r\r\r"
   - Expected Output: The `firstName` field should not be set.

8. **Test with a string containing only line feeds**:
   - Input: "\f\f\f"
   - Expected Output: The `firstName` field should not be set.

9. **Test with a string containing only vertical tabs**:
   - Input: "\v\v\v"
   - Expected Output: The `firstName` field should not be set.

10. **Test with a string containing only form feeds**:
    - Input: "\f\f\f"
    - Expected Output: The `firstName` field should not be set.

These test scenarios cover a variety of cases to ensure that the `setFirstName` method behaves as expected in different scenarios.

*/
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Assertions;
import org.mockito.Mockito;
import org.springframework.samples.petclinic.model.Person;

public class Person_setFirstName_8acaec2cfc_Test {

    @Test
    public void testSetFirstName_success() {
        // Arrange
        Person person = new Person();
        String expectedFirstName = "John";

        // Act
        person.setFirstName(expectedFirstName);

        // Assert
        Assertions.assertEquals(expectedFirstName, person.getFirstName());
    }

    @Test
    public void testSetFirstName_null() {
        // Arrange
        Person person = new Person();

        // Act
        person.setFirstName(null);

        // Assert
        Assertions.assertNull(person.getFirstName());
    }
}
