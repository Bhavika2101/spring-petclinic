/*
Test generated by RoostGPT for test pom-test using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat

Sure, here are some test scenarios that you can use to validate the business logic of the `setLastName` method:

1. **Test with a valid last name**:
   - Input: "Smith"
   - Expected Output: The `lastName` field should be set to "Smith".

2. **Test with an empty last name**:
   - Input: ""
   - Expected Output: An exception should be thrown because the `lastName` field cannot be blank.

3. **Test with a null last name**:
   - Input: null
   - Expected Output: An exception should be thrown because the `lastName` field cannot be null.

4. **Test with a last name containing only spaces**:
   - Input: "   "
   - Expected Output: An exception should be thrown because the `lastName` field cannot be blank.

5. **Test with a last name containing special characters**:
   - Input: "O'Neill"
   - Expected Output: The `lastName` field should be set to "O'Neill".

6. **Test with a last name containing numbers**:
   - Input: "John123"
   - Expected Output: An exception should be thrown because the `lastName` field cannot contain numbers.

7. **Test with a last name containing uppercase letters**:
   - Input: "SMITH"
   - Expected Output: The `lastName` field should be set to "SMITH".

8. **Test with a last name containing lowercase letters**:
   - Input: "smith"
   - Expected Output: The `lastName` field should be set to "smith".

Remember, these test scenarios are based on the assumption that the `setLastName` method is part of a larger class and that the `lastName` field is annotated with `@NotBlank` and `@Column` annotations. If the `setLastName` method is not part of a larger class or if the `lastName` field is not annotated with these annotations, you will need to adjust the test scenarios accordingly.

*/
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Assertions;
import org.mockito.Mockito;
import org.springframework.samples.petclinic.model.Person;

public class Person_setLastName_4141db5e7c_Test {

    @Test
    public void testSetLastName_success() {
        // Arrange
        Person person = new Person();
        String expectedLastName = "Smith";

        // Act
        person.setLastName(expectedLastName);

        // Assert
        Assertions.assertEquals(expectedLastName, person.getLastName());
    }

    @Test
    public void testSetLastName_null() {
        // Arrange
        Person person = new Person();

        // Act
        person.setLastName(null);

        // Assert
        Assertions.assertNull(person.getLastName());
    }
}
