# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /my/payment/simulation_post for http method type POST in artillery framework
# RoostTestHash=b2c14d9fe3
# 
# 

# ********RoostGPT********
config:
  target: "http://localhost:3000"  # Replace with your actual API host
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "my_payment_simulation_post_others.csv"
    fields: ["merchant", "order", "system", "transaction"]
    skipHeader: true

scenarios:
  - name: "Non-success API responses"
    flow:
      # Invalid URL
      - get:
          url: "/invalid/url"
          expect:
            - statusCode: 404

      # Missing Authorization header
      - post:
          url: "/my/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
          json:
            merchant: "{{ merchant }}"
            order: "{{ order }}"
            system: "{{ system }}"
            transaction: "{{ transaction }}"
          expect:
            - statusCode: 401

      # Invalid payload
      - post:
          url: "/my/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"  # Ensure this environment variable is defined
          json:
            merchant: "invalid"
            order: "invalid"
            system: "invalid"
            transaction: "invalid"
          expect:
            - statusCode: 400

      - think: 1
