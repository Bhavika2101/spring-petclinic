# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /my/payment/simulation_post for http method type POST in artillery framework
# RoostTestHash=b2c14d9fe3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    path: "my_payment_simulation_post_others.csv"
    fields: ["merId", "amount", "currency", "txnRef", "txnChannel", "referenceLabel", "is_notification_encrypted"]
    skipHeader: true

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      - get:
          url: "/wrong/url"
          expect:
            - statusCode: 404
      - think: 1
      - post:
          url: "/my/payment/simulation"
          json:
            merchant:
              merId: "{{ merId }}"
            order:
              amount: "{{ amount }}"
              currency: "{{ currency }}"
            transaction:
              txnRef: "{{ txnRef }}"
              txnChannel: "{{ txnChannel }}"
              referenceLabel: "{{ referenceLabel }}"
            system:
              is_notification_encrypted: "{{ is_notification_encrypted }}"
          expect:
            - statusCode: 401
      - think: 1
      - post:
          url: "/my/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: "false"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            wrongField: "wrongValue"
          expect:
            - statusCode: 400
      - think: 1
      - post:
          url: "/my/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: "false"
            Authorization: "Bearer wrongToken"
          json:
            merchant:
              merId: "{{ merId }}"
            order:
              amount: "{{ amount }}"
              currency: "{{ currency }}"
            transaction:
              txnRef: "{{ txnRef }}"
              txnChannel: "{{ txnChannel }}"
              referenceLabel: "{{ referenceLabel }}"
            system:
              is_notification_encrypted: "{{ is_notification_encrypted }}"
          expect:
            - statusCode: 403
      - think: 1
