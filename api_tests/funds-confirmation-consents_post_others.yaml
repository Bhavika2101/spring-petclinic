# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /funds-confirmation-consents_post for http method type POST in artillery framework
# RoostTestHash=98c0977e89
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "funds-confirmation-consents_post_others.csv"
    fields:
      - "ExpirationDateTime"
      - "SchemeName"
      - "Identification"
      - "Name"
      - "SecondaryIdentification"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
scenarios:
  - name: "Negative test scenarios for API requests"
    flow:
      # Test with invalid headers
      - post:
          url: "/funds-confirmation-consents"
          headers:
            x-fapi-auth-date: "invalid"
            x-fapi-customer-ip-address: "invalid"
            x-fapi-interaction-id: "invalid"
            Authorization: "invalid"
            x-customer-user-agent: "invalid"
          json:
            Data:
              ExpirationDateTime: "{{ ExpirationDateTime }}"
              DebtorAccount:
                SchemeName: "{{ SchemeName }}"
                Identification: "{{ Identification }}"
                Name: "{{ Name }}"
                SecondaryIdentification: "{{ SecondaryIdentification }}"
          expect:
            - statusCode: 401
      - think: 1

      # Test with invalid URL
      - post:
          url: "/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            Data:
              ExpirationDateTime: "{{ ExpirationDateTime }}"
              DebtorAccount:
                SchemeName: "{{ SchemeName }}"
                Identification: "{{ Identification }}"
                Name: "{{ Name }}"
                SecondaryIdentification: "{{ SecondaryIdentification }}"
          expect:
            - statusCode: 404
      - think: 1

      # Test with invalid payload
      - post:
          url: "/funds-confirmation-consents"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            Data:
              ExpirationDateTime: "invalid"
              DebtorAccount:
                SchemeName: "invalid"
                Identification: "invalid"
                Name: "invalid"
                SecondaryIdentification: "invalid"
          expect:
            - statusCode: 400
      - think: 1
