# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /funds-confirmations_post for http method type POST in artillery framework
# RoostTestHash=e31facdc08
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "funds-confirmations_post_others.csv"
    fields:
      - "Data.ConsentId"
      - "Data.Reference"
      - "Data.InstructedAmount.Amount"
      - "Data.InstructedAmount.Currency"
    skipHeader: true
  ensure:
    - p(95)<200
    - maxErrorRate<1
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Non-success API responses scenario"
    flow:
      # Invalid POST request due to missing required Authorization header
      - post:
          url: "/funds-confirmations"
          headers:
            x-fapi-auth-date: "{{$timestamp}}"
            x-fapi-customer-ip-address: "{{$randomIP}}"
            x-fapi-interaction-id: "{{$randomString}}"
            x-customer-user-agent: "{{$randomUserAgent}}"
          json:
            Data:
              ConsentId: "{{Data.ConsentId}}"
              Reference: "{{Data.Reference}}"
              InstructedAmount:
                Amount: "{{Data.InstructedAmount.Amount}}"
                Currency: "{{Data.InstructedAmount.Currency}}"
          expect:
            - statusCode: 401
      - think: 1

      # Invalid POST request due to incorrect API endpoint
      - post:
          url: "/invalid-endpoint"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "{{$timestamp}}"
            x-fapi-customer-ip-address: "{{$randomIP}}"
            x-fapi-interaction-id: "{{$randomString}}"
            x-customer-user-agent: "{{$randomUserAgent}}"
          json:
            Data:
              ConsentId: "{{Data.ConsentId}}"
              Reference: "{{Data.Reference}}"
              InstructedAmount:
                Amount: "{{Data.InstructedAmount.Amount}}"
                Currency: "{{Data.InstructedAmount.Currency}}"
          expect:
            - statusCode: 404
      - think: 1
