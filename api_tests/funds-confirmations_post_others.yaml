# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /funds-confirmations_post for http method type POST in artillery framework
# RoostTestHash=e31facdc08
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "funds-confirmations_post_others.csv"
    fields: ["ConsentId", "Reference", "Amount", "Currency"]
    skipHeader: true

scenarios:
  - name: "Test non-successful API requests"
    flow:
      - post: # Testing invalid payload
          url: "/funds-confirmations"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "invalid-date"
            x-fapi-customer-ip-address: "invalid-ip"
            x-fapi-interaction-id: "invalid-id"
            x-customer-user-agent: "invalid-user-agent"
          json:
            Data:
              ConsentId: "{{ ConsentId }}"
              Reference: "{{ Reference }}"
              InstructedAmount:
                Amount: "{{ Amount }}"
                Currency: "{{ Currency }}"
          expect:
            - statusCode: 400
      - think: 1

      - post: # Testing invalid URL
          url: "/invalid-url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            Data:
              ConsentId: "{{ ConsentId }}"
              Reference: "{{ Reference }}"
              InstructedAmount:
                Amount: "{{ Amount }}"
                Currency: "{{ Currency }}"
          expect:
            - statusCode: 404
      - think: 1

      - post: # Testing without authorization
          url: "/funds-confirmations"
          json:
            Data:
              ConsentId: "{{ ConsentId }}"
              Reference: "{{ Reference }}"
              InstructedAmount:
                Amount: "{{ Amount }}"
                Currency: "{{ Currency }}"
          expect:
            - statusCode: 401
      - think: 1

      - post: # Testing with invalid authorization token
          url: "/funds-confirmations"
          headers:
            Authorization: "Bearer invalid-token"
          json:
            Data:
              ConsentId: "{{ ConsentId }}"
              Reference: "{{ Reference }}"
              InstructedAmount:
                Amount: "{{ Amount }}"
                Currency: "{{ Currency }}"
          expect:
            - statusCode: 403
      - think: 1
