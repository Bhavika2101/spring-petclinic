# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /sg/payment/simulation_post for http method type POST in artillery framework
# RoostTestHash=261d0e9dce
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "sg_payment_simulation_post_success.csv"
    fields:
      - "txnRef"
      - "merId"
      - "currency"
      - "amount"
      - "originatingCustName"
      - "is_notification_encrypted"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Singapore PayNow Payment Simulation API"
    flow:
      - post:
          url: "/sg/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            currency: "{{ currency }}"
            amount: "{{ amount }}"
            originatingCustName: "{{ originatingCustName }}"
            is_notification_encrypted: "{{ is_notification_encrypted }}"
          expect:
            - statusCode: 200
            - hasProperty: "api_gw"
            - hasProperty: "response"
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
            - hasProperty: ["api_gw.messageId", "api_gw.returnCode", "api_gw.returnReason", "api_gw.sentTime", "api_gw.responseTime"]
            - hasProperty: ["response.txnRef", "response.proCode", "response.proMsg"]
      - think: 1
