# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /hk/refund/simulation_post for http method type POST in artillery framework
# RoostTestHash=8f41f0dbce
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "hk_refund_simulation_post_success.csv"
    fields:
      - "txnRef"
      - "merId"
      - "bankTxnId"
      - "txnAmt"
      - "refundAmt"
      - "notifyUrl"
    skipHeader: true

scenarios:
  - name: "Simulate refund requests"
    flow:
      # Post request to simulate refund
      - post:
          url: "/hk/refund/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            bankTxnId: "{{ bankTxnId }}"
            txnAmt: "{{ txnAmt }}"
            refundAmt: "{{ refundAmt }}"
            notifyUrl: "{{ notifyUrl }}"
          expect:
            - statusCode: 200
            - hasProperty: "api_gw"
            - hasProperty: "response"
            - hasHeader: "Content-Type"
            - headerEquals: ["Content-Type", "application/json"]
          think: 1
