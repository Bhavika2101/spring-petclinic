# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /hk/refund/simulation_post for http method type POST in artillery framework
# RoostTestHash=8f41f0dbce
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "hk_refund_simulation_post_success.csv"
    fields:
      - "txnRef"
      - "merId"
      - "bankTxnId"
      - "txnAmt"
      - "refundAmt"
      - "notifyUrl"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API responses"
    flow:
      - post:
          url: "/hk/refund/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: "false"
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            bankTxnId: "{{ bankTxnId }}"
            txnAmt: "{{ txnAmt }}"
            refundAmt: "{{ refundAmt }}"
            notifyUrl: "{{ notifyUrl }}"
          expect:
            - statusCode: 200
            - hasProperty: "api_gw.messageId"
            - hasProperty: "api_gw.returnCode"
            - equals: ["api_gw.returnCode", "200"]
            - hasProperty: "api_gw.returnReason"
            - hasProperty: "api_gw.sentTime"
            - hasProperty: "api_gw.responseTime"
            - hasProperty: "response.bankTxnId"
            - hasProperty: "response.proCode"
            - hasProperty: "response.proMsg"
      - think: 1
