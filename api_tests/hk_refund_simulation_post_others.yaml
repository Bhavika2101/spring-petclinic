# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /hk/refund/simulation_post for http method type POST in artillery framework
# RoostTestHash=8f41f0dbce
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "hk_refund_simulation_post_others.csv"
    fields:
      - "txnRef"
      - "merId"
      - "bankTxnId"
      - "txnAmt"
      - "refundAmt"
      - "notifyUrl"
    skipHeader: true

scenarios:
  - name: "Non-success API responses testing"
    flow:
      - post:
          url: "/hk/refund/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            bankTxnId: "{{ bankTxnId }}"
            txnAmt: "invalid_amount"
            refundAmt: "{{ refundAmt }}"
            notifyUrl: "{{ notifyUrl }}"
          expect:
            - statusCode: 400
      - think: 1

      - post:
          url: "/hk/refund/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "invalid_token"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            bankTxnId: "{{ bankTxnId }}"
            txnAmt: "{{ txnAmt }}"
            refundAmt: "{{ refundAmt }}"
            notifyUrl: "{{ notifyUrl }}"
          expect:
            - statusCode: 401
      - think: 1

      - post:
          url: "/hk/refund/invalid_simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            bankTxnId: "{{ bankTxnId }}"
            txnAmt: "{{ txnAmt }}"
            refundAmt: "{{ refundAmt }}"
            notifyUrl: "{{ notifyUrl }}"
          expect:
            - statusCode: 404
      - think: 1
