# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /funds-confirmation-consents/{ConsentId}_delete for http method type DELETE in artillery framework
# RoostTestHash=9c17cf291d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "funds-confirmation-consents_ConsentId_delete_others.csv"
    fields:
      - "ConsentId"
    skipHeader: true

scenarios:
  - name: "Non-Success API Responses Test Scenario"
    flow:
      - get:
          url: "/invalid-url"
          expect:
            - statusCode: 404
      - think: 1
      - post:
          url: "/funds-confirmation-consents/{ConsentId}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "{{ $isoTimestamp() }}"
            x-fapi-customer-ip-address: "192.0.2.0"
            x-fapi-interaction-id: "{{ $uuid() }}"
            x-customer-user-agent: "Mozilla/5.0"
          json:
            invalidField: "invalidValue"
          expect:
            - statusCode: 400
      - think: 1
      - post:
          url: "/funds-confirmation-consents/{ConsentId}"
          headers:
            Authorization: ""
            x-fapi-auth-date: "{{ $isoTimestamp() }}"
            x-fapi-customer-ip-address: "192.0.2.0"
            x-fapi-interaction-id: "{{ $uuid() }}"
            x-customer-user-agent: "Mozilla/5.0"
          json:
            validField: "validValue"
          expect:
            - statusCode: 401
      - think: 1
      - post:
          url: "/funds-confirmation-consents/{ConsentId}"
          headers:
            Authorization: "Bearer invalidToken"
            x-fapi-auth-date: "{{ $isoTimestamp() }}"
            x-fapi-customer-ip-address: "192.0.2.0"
            x-fapi-interaction-id: "{{ $uuid() }}"
            x-customer-user-agent: "Mozilla/5.0"
          json:
            validField: "validValue"
          expect:
            - statusCode: 403
      - think: 1
