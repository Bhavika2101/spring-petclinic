# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /funds-confirmation-consents/{ConsentId}_delete for http method type DELETE in artillery framework
# RoostTestHash=9c17cf291d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "funds-confirmation-consents_ConsentId_delete_success.csv"
    fields: ["ConsentId"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "API Test Scenario"
    flow:
      # Delete Funds Confirmation Consent
      - delete:
          url: "/funds-confirmation-consents/{{ ConsentId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            x-fapi-auth-date: "{{ $processEnvironment.AUTH_DATE }}"
            x-fapi-customer-ip-address: "{{ $processEnvironment.CUSTOMER_IP }}"
            x-fapi-interaction-id: "{{ $processEnvironment.INTERACTION_ID }}"
            x-customer-user-agent: "{{ $processEnvironment.USER_AGENT }}"
          expect:
            - statusCode: 204
            - hasHeader: "x-fapi-interaction-id"
      - think: 1
