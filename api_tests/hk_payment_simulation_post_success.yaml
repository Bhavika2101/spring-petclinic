# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /hk/payment/simulation_post for http method type POST in artillery framework
# RoostTestHash=27949d3d45
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "hk_payment_simulation_post_success.csv"
    fields:
      - "txnRef"
      - "merId"
      - "is_notification_encrypted"
      - "for_bill_payment"
      - "for_nonbill_payment"
  plugins:
    expect: {}

scenarios:
  - name: "HK Payment Simulation API Test"
    flow:
      - post:
          url: "/hk/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: "false"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            is_notification_encrypted: "{{ is_notification_encrypted }}"
            for_bill_payment: "{{ for_bill_payment }}"
            for_nonbill_payment: "{{ for_nonbill_payment }}"
      - think: 1
      - expect:
          - statusCode: 200
          - hasProperty: "api_gw"
          - hasProperty: "response"
          - hasProperty: "api_gw.messageId"
          - hasProperty: "api_gw.returnCode"
          - equals: ["api_gw.returnCode", "200"]
          - hasProperty: "api_gw.returnReason"
          - hasProperty: "api_gw.sentTime"
          - hasProperty: "api_gw.responseTime"
          - hasProperty: "response.txnRef"
          - equals: ["response.txnRef", "{{ txnRef }}"]
          - hasProperty: "response.proCode"
          - hasProperty: "response.proMsg"
          - hasProperty: "response.currency"
