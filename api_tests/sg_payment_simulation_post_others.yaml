# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /sg/payment/simulation_post for http method type POST in artillery framework
# RoostTestHash=261d0e9dce
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "sg_payment_simulation_post_others.csv"
    fields:
      - txnRef
      - merId
      - currency
      - amount
      - originatingCustName
      - is_notification_encrypted
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API responses"
    flow:
      # Testing invalid payload
      - post:
          url: "/sg/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            txnRef: "invalid_txnRef"
            merId: "invalid_merId"
            currency: "invalid_currency"
            amount: 0
            originatingCustName: "invalid_originatingCustName"
            is_notification_encrypted: "invalid_is_notification_encrypted"
      - think: 1
      - expect:
          - statusCode: 400

      # Testing invalid URL
      - get:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1
      - expect:
          - statusCode: 404

      # Testing without authorization token
      - get:
          url: "/sg/payment/simulation"
      - think: 1
      - expect:
          - statusCode: 401

      # Testing with invalid authorization token
      - get:
          url: "/sg/payment/simulation"
          headers:
            Authorization: "invalid_token"
      - think: 1
      - expect:
          - statusCode: 403
