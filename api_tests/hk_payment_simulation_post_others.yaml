# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /hk/payment/simulation_post for http method type POST in artillery framework
# RoostTestHash=27949d3d45
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "hk_payment_simulation_post_others.csv"
    fields:
      - "txnRef"
      - "merId"
      - "is_notification_encrypted"
      - "currency"
      - "amount"
      - "suppInfo"
    skipHeader: true

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      # Testing with invalid URL
      - get:
          url: "/invalid/url"
          expect:
            - statusCode: 404
          think: 1

      # Testing with invalid payload
      - post:
          url: "/hk/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: "false"
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            is_notification_encrypted: "{{ is_notification_encrypted }}"
            for_bill_payment:
              currency: "{{ currency }}"
              amount: "{{ amount }}"
              suppInfo: "Invalid SuppInfo"
          expect:
            - statusCode: 400
          think: 1

      # Testing with missing authorization token
      - post:
          url: "/hk/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: "false"
            Authorization: ""
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            is_notification_encrypted: "{{ is_notification_encrypted }}"
            for_nonbill_payment:
              currency: "{{ currency }}"
              amount: "{{ amount }}"
              suppInfo: "{{ suppInfo }}"
          expect:
            - statusCode: 401
          think: 1

      # Testing with invalid method type
      - put:
          url: "/hk/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: "false"
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            is_notification_encrypted: "{{ is_notification_encrypted }}"
            for_nonbill_payment:
              currency: "{{ currency }}"
              amount: "{{ amount }}"
              suppInfo: "{{ suppInfo }}"
          expect:
            - statusCode: 405
          think: 1
