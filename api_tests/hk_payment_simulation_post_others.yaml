# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /hk/payment/simulation_post for http method type POST in artillery framework
# RoostTestHash=27949d3d45
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "hk_payment_simulation_post_others.csv"
    fields: ["txnRef", "merId", "is_notification_encrypted", "for_bill_payment", "for_nonbill_payment"]
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      - post:
          url: "/invalid_url"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            is_notification_encrypted: "{{ is_notification_encrypted }}"
            for_bill_payment: "{{ for_bill_payment }}"
            for_nonbill_payment: "{{ for_nonbill_payment }}"
          expect:
            - statusCode: 404
      - think: 1

      - post:
          url: "/hk/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "Bearer InvalidToken"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            is_notification_encrypted: "{{ is_notification_encrypted }}"
            for_bill_payment: "{{ for_bill_payment }}"
            for_nonbill_payment: "{{ for_nonbill_payment }}"
          expect:
            - statusCode: 401
      - think: 1

      - post:
          url: "/hk/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            is_notification_encrypted: "{{ is_notification_encrypted }}"
            for_bill_payment: null
            for_nonbill_payment: null
          expect:
            - statusCode: 400
      - think: 1

      - post:
          url: "/hk/payment/simulation"
          headers:
            Content-Type: "application/json"
            message_encrypt: false
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            txnRef: "{{ txnRef }}"
            merId: "{{ merId }}"
            is_notification_encrypted: "InvalidValue"
            for_bill_payment: "{{ for_bill_payment }}"
            for_nonbill_payment: "{{ for_nonbill_payment }}"
          expect:
            - statusCode: 400
      - think: 1
