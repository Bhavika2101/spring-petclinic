# ********RoostGPT********

# Test generated by RoostGPT for test hsbc-simulation-api using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /funds-confirmation-consents/{ConsentId}_get for http method type GET in artillery framework
# RoostTestHash=d59c33036c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "funds-confirmation-consents_ConsentId_get_success.csv"
    fields:
      - ConsentId
    skipHeader: true

scenarios:
  - name: "Test Scenario - Successful API requests"
    flow:
      - get:
          url: "/funds-confirmation-consents/{{ ConsentId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            x-fapi-auth-date: "{{$processEnvironment.AUTH_DATE}}"
            x-fapi-customer-ip-address: "{{$processEnvironment.CUSTOMER_IP}}"
            x-fapi-interaction-id: "{{$processEnvironment.INTERACTION_ID}}"
            x-customer-user-agent: "{{$processEnvironment.USER_AGENT}}"
          expect:
            - statusCode: 200
            - hasProperty: "Data"
            - hasProperty: "Data.ConsentId"
            - hasProperty: "Data.CreationDateTime"
            - hasProperty: "Data.Status"
            - hasProperty: "Data.StatusUpdateDateTime"
            - hasProperty: "Data.ExpirationDateTime"
            - hasProperty: "Data.DebtorAccount"
            - hasProperty: "Data.DebtorAccount.SchemeName"
            - hasProperty: "Data.DebtorAccount.Identification"
            - hasProperty: "Data.DebtorAccount.Name"
            - hasProperty: "Data.DebtorAccount.SecondaryIdentification"
            - hasProperty: "Links"
            - hasProperty: "Links.Self"
            - hasHeader: "x-fapi-interaction-id"
          think: 1
